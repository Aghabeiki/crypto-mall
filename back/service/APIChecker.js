module.exports = function (req, res, next) {
    next();
    // const apiKey = req.header('api-key');
    // if (apiKey == undefined || apiKey != 'test') {
    //     res.status(403).json({error_message: 'you have not access to this page'})
    //    // next()
    // }
    // else {
    //     next()
    // }
}